/*# Exercise 38

Starting from the exercise 37, print the key of the `car` object.

**Suggestion**

Use the `for in` statement
 */
let car = {
    name: "Ford",
    color: "Red"
};
//lo hacemos de esta manera para que solo salga el nombre y el color,
//si queremos imprimir todo lo que tiene car haremos console.log(car);
console.log(car.name);
console.log(car.color);
